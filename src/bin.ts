#!/usr/bin/env node

/* eslint-disable no-console -- TODO */
import boxen from "boxen";

console.log(
  boxen("▲   Vercel CRON   ▲", {
    borderColor: "magenta",
    borderStyle: "round",
    padding: 1,
  })
);
